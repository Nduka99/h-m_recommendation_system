# Virtual Environments
.hnm/
.hnm_backup/
.hnm_py312/
venv/
env/
.env

# Python Cache
__pycache__/
*.pyc

# Large Data & Artifacts
*.parquet
*.duckdb
*.index
*.pkl
*.model
*.csv
*.zip

# Ignore data folder (heavy local data)
data/

# Backend Artifacts (Include lightweight model files needed for docker build)
!backend/artifacts/*.parquet
!backend/artifacts/*.txt
!backend/artifacts/*.pkl
# Trigger ignore for specific large files if they accidentally get in
backend/artifacts/*.index

# Verification Scripts (Keep these ignored if they are just local tools, or track them if needed. Un-comment to track)
# scripts/verify_*.py
# scripts/debug_*.py

