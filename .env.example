# H&M Recommendation System - Environment Variables

# --- Docker Compose / Service Config ---
# Ensure these match your docker-compose service names and ports

# Backend
# When running via docker-compose, frontend talks to backend service name
API_URL=http://backend:8000

# Artifacts
# Inside the container, artifacts are usually copied to /app/artifacts or /app/backend/artifacts
# For our structure, we copy them to /app/artifacts in the backend container
ARTIFACT_DIR=artifacts

# Image Hosting
# Can be a local path or web URL.
# If using local images, map the volume in docker-compose
IMAGE_BASE_PATH=/app/images

# Optional Debugging
DEBUG=True
